name: Build & Publish to Marketplace

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mkdocs.yml"
  workflow_dispatch:

jobs:
  publish-techdocs-site:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    env:
      TECHDOCS_S3_BUCKET_NAME: ${{ vars.MARKETPLACE_TECHDOCS_S3_BUCKET_NAME }}
      AWS_ACCOUNT: ${{ vars.MARKETPLACE_AWS_ACCOUNT }}
      AWS_REGION: ${{ vars.MARKETPLACE_AWS_REGION }}
      ENTITY_NAMESPACE: default
      ENTITY_KIND: mapfredocument
      ENTITY_NAME: ${{ vars.ENTITY_NAME }}
      TECHDOCS_ROLE_NAME: ${{ vars.MARKETPLACE_TECHDOCS_ROLE_NAME }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT }}:role/${{ env.TECHDOCS_ROLE_NAME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install techdocs-cli
        run: sudo npm install -g @techdocs/cli

      - name: Install mkdocs and mkdocs plugins
        run: python -m pip install mkdocs-techdocs-core==1.*

      - name: Generate docs site
        run: techdocs-cli generate --no-docker --verbose

      - name: Publish docs site
        run: techdocs-cli publish --publisher-type awsS3 --storage-name $TECHDOCS_S3_BUCKET_NAME --entity $ENTITY_NAMESPACE/$ENTITY_KIND/$ENTITY_NAME
